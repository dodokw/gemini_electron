# GeminiApp

React, Electron, Express.js를 기반으로 구축된 데스크톱 애플리케이션입니다. Prisma ORM을 사용하여 PostgreSQL 데이터베이스와 연동되며, Swagger를 통한 API 문서화 및 `node-pty`를 이용한 터미널 통합 기능을 제공합니다.

## 🚀 주요 기능

- **Electron 데스크톱 앱**: React 기반의 사용자 인터페이스를 제공하는 Electron 애플리케이션입니다.
- **Express.js 백엔드**: RESTful API 서버를 내장하고 있습니다.
- **데이터베이스 연동**: Prisma ORM을 사용하여 PostgreSQL 데이터베이스를 쉽고 안전하게 관리합니다.
- **인증 시스템**: JWT(Json Web Token)와 BCrypt를 사용하여 안전한 로그인 및 회원가입 기능을 제공합니다.
- **터미널 통합**: `node-pty`를 사용하여 애플리케이션 내부에서 CLI 명령(Gemini CLI 등)을 실행하고 결과를 확인할 수 있습니다.
- **API 문서화**: `swagger-autogen` 및 `swagger-ui-express`를 사용하여 `/api-docs` 경로에서 API 명세를 자동으로 제공합니다.

## 🛠️ 사전 요구 사항 (Prerequisites)

이 프로젝트를 실행하기 위해서는 다음 소프트웨어가 설치되어 있어야 합니다.

- [Node.js](https://nodejs.org/) (v18 이상 권장)
- [PostgreSQL](https://www.postgresql.org/)
- **Gemini CLI**: 터미널 통합 기능을 사용하기 위해 전역으로 설치되어 있어야 합니다.
  ```bash
  npm install -g @google/gemini-cli
  ```

## 📦 설치 방법 (Installation)

1. 저장소를 클론합니다.

2. 루트 디렉토리의 의존성을 설치합니다.
   ```bash
   npm install
   ```

3. Frontend (`gemini-app`) 의존성을 설치합니다.
   ```bash
   cd gemini-app
   npm install
   cd ..
   ```

4. 데이터베이스 및 Prisma 설정
   - 데이터베이스가 실행 중이어야 합니다.
   - `npx prisma generate` 등을 통해 Prisma 클라이언트를 생성합니다.

## ⚙️ 환경 변수 설정 (Environment Variables)

`src/dev.env` 파일 또는 `.env` 파일을 생성하여 다음과 같은 환경 변수를 설정해야 합니다. (보안상 저장소에는 포함되지 않습니다.)

```env
PORT=8000
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
JWT_SECRET_KEY="your_secret_key"
```
> **참고**: `server.js`는 기본적으로 `src/dev.env` 경로의 환경 변수 파일을 로드하도록 설정되어 있습니다.

## ▶️ 실행 방법 (Usage)

### 개발 모드 전체 실행
React 앱, 백엔드 서버, Electron을 동시에 실행합니다.
```bash
npm start
```

### 개별 실행 명령어
필요에 따라 각 부분을 따로 실행할 수 있습니다.

- **React 앱 실행**:
  ```bash
  npm run react-start
  ```
- **백엔드 서버 실행**:
  ```bash
  npm run server-start
  ```
- **Electron 실행**:
  ```bash
  npm run electron-start
  ```
- **빌드 (Build)**:
  Swagger 문서 생성, React 빌드, Electron 빌드를 수행합니다.
  ```bash
  npm run build
  ```

## 📖 API 문서 (Swagger)
서버가 실행 중일 때 브라우저 또는 Electron 앱 내에서 다음 주소로 접속하여 API 문서를 확인할 수 있습니다.
- URL: `http://localhost:8000/api-docs`

## 📂 디렉토리 구조

```
/my-project
├── node_modules/           # 설치된 라이브러리들
├── prisma/                 # Prisma 스키마 및 마이그레이션 파일
│
├── gemini-app/             # React 프론트엔드 프로젝트
│
├── src/                    # 백엔드 소스 코드
│ ├── routes/               # API 라우트 정의 (엔드포인트)
│ │ ├── index.js            # 라우터 통합
│ │ ├── users.routes.js     # 사용자 관련 API
│ │ └── gemini.routes.js    # CLI/터미널 관련 API
│ │
│ ├── controllers/          # 요청/응답 처리 로직
│ │ ├── users.controller.js
│ │ └── gemini.controller.js
│ │
│ ├── services/             # 비즈니스 로직 및 DB 처리
│ │ ├── users.service.js
│ │ └── gemini.service.js
│ │
│ ├── middlewares/          # 미들웨어 (예: JWT 인증)
│ │ └── auth.middleware.js
│ │
│ ├── config/               # 설정 파일
│ │ └── config.js
│ │
│ └── app.js                # Express 앱 설정 (Cors, Swagger 등)
│
├── .env                    # 환경 변수 (Git 제외)
├── server.js               # 서버 진입점
└── package.json            # 프로젝트 설정 및 스크립트
```
